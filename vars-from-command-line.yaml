- name: command line input
  hosts: my_ip
  connection: local
  tasks:
  - name: printing command line variables
    ansible.builtin.debug:
     msg: My name is {{ NAME }} and I'm {{ AGE }} years old.